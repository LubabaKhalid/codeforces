//1st code 
n,m,a=map(int,input().split())
x=n//a
y=m//a
if n%a!=0:
    x=x+1
if m%a!=0:
    y=y+1
print(x*y)


//2nd code
import math
n, m, a = map(int, input().split())
h = math.ceil(n / a)
v = math.ceil(m / a)
flags = h * v
print(flags)
